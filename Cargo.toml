[package]
name = "mathroborust"
version = "0.1.0"
edition = "2024"

[dependencies]
nalgebra = { version = "0.32", features = ["serde-serialize"] }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
